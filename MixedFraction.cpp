#include <iostream>
using namespace std;

int main() {
    // Given two numbers, numerator and denominator, the numerator is definitely larger than the denominator, print the mixed fraction form of them.

    int numerator=5;
    int denominator=2;
    int whole_num = n/d;
    int fraction = n%d;
    if(frac>0) cout<<whole_num<<" "<<fraction<<"/"<<denominator;
    else cout<<whole_num;
    return 0;
}
